{{- /*
The `{{ ... }}` blocks below are External Secrets templates, evaluated by the
ESO controller at runtime — not by Helm. They're wrapped in Helm raw-string
literals (backticks) so Helm passes them through verbatim instead of trying to
resolve them at template time.
*/ -}}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: bjj-mongodb-root-password
  namespace: {{ .Values.envId }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: azure-keyvault-store
  target:
    name: bjj-mongodb-root-password
    creationPolicy: Owner
    template:
      type: Opaque
      data:
        mongodb-password: '{{ `{{ .mongodb_password }}` }}'
  data:
    - secretKey: mongodb_password
      remoteRef:
        key: bjj-mongodb-root-password
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: bjj-azure-ad-secret
  namespace: {{ .Values.envId }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: azure-keyvault-store
  target:
    name: bjj-azure-ad-secret
    creationPolicy: Owner
    template:
      type: Opaque
      data:
        AzureAd__TenantId: '{{ `{{ .tenant_id }}` }}'
        AzureAd__ClientId: '{{ `{{ .client_id }}` }}'
        AzureAd__Audience: '{{ `{{ .audience }}` }}'
  data:
    - secretKey: tenant_id
      remoteRef:
        key: bjj-api-azuread-tenant-id
    - secretKey: client_id
      remoteRef:
        key: bjj-api-azuread-client-id
    - secretKey: audience
      remoteRef:
        key: bjj-api-azuread-audience
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ghcr-pull-secret
  namespace: {{ .Values.envId }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: azure-keyvault-store
  target:
    name: ghcr-pull-secret
    creationPolicy: Owner
    template:
      type: kubernetes.io/dockerconfigjson
      data:
        .dockerconfigjson: |
          {"auths":{"ghcr.io":{"auth":"{{ `{{ printf "ianoflynnautomation:%s" .ghcr_pat | b64enc }}` }}"}}}
  data:
    - secretKey: ghcr_pat
      remoteRef:
        key: ghcr-pat
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: bjj-donation-secret
  namespace: {{ .Values.envId }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: azure-keyvault-store
  target:
    name: bjj-donation-secret
    creationPolicy: Owner
    template:
      type: Opaque
      data:
        Donation__BitcoinAddress: '{{ `{{ .bitcoin_address }}` }}'
  data:
    - secretKey: bitcoin_address
      remoteRef:
        key: bjj-donation-bitcoin-address
